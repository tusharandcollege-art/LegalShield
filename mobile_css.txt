    /* ── MOBILE APP ≤ 768px ── */
    @media (max-width: 768px) {
      .navbar { display: none !important; }
      
      /* Base App Shell */
      body { 
        padding-top: 72px; 
        padding-bottom: calc(68px + env(safe-area-inset-bottom, 16px)); 
        overflow-x: hidden; 
      }

      /* APP HEADER */
      .mob-header {
        display: flex; position: fixed; top: 0; left: 0; right: 0;
        height: 72px; padding: 0 20px; background: rgba(15, 15, 15, 0.95);
        backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
        border-bottom: 1px solid rgba(255,255,255,0.05);
        align-items: center; justify-content: space-between; z-index: 9999;
      }
      .mob-header-left  { display: flex; align-items: center; gap: 12px; }
      .mob-header-right { display: flex; align-items: center; gap: 12px; }
      .mob-logo-box {
        width: 42px; height: 42px; border-radius: 12px;
        background: #1a1a1a; border: 1px solid rgba(212,160,23,0.3);
        display: flex; align-items: center; justify-content: center;
      }
      .mob-logo-box svg { width: 22px; height: 22px; }
      .mob-logo-info { display: flex; flex-direction: column; }
      .mob-logo-name { font-size: 14px; font-weight: 700; color: #fff; line-height: 1.2; }
      .mob-logo-name em { color: var(--gold); font-style: normal; }
      .mob-logo-sub { font-size: 10px; color: #6a6a7a; margin-top: 2px; }
      .mob-icon-btn {
        width: 40px; height: 40px; border-radius: 50%;
        background: #1c1c1c; border: 1px solid rgba(255,255,255,0.08);
        display: flex; align-items: center; justify-content: center;
        text-decoration: none; cursor: pointer;
      }
      .mob-icon-btn svg { width: 18px; height: 18px; stroke: #ccc; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }

      /* COMMON HORIZONTAL SCROLLER PATTERN */
      .slider-container {
        display: flex !important; flex-wrap: nowrap !important;
        overflow-x: auto !important; gap: 16px !important;
        padding: 8px 24px 24px !important; margin: 0 -24px !important;
        -webkit-overflow-scrolling: touch; scrollbar-width: none;
      }
      .slider-container::-webkit-scrollbar { display: none; }
      .slider-item { flex: 0 0 80vw !important; border-radius: 16px; }

      /* SECTIONS BASE (Unified App Spacing) */
      .process-section, .works-section, .services-section,
      .about-section, .media-section, .testimonials-section {
        padding: 48px 24px; overflow: hidden;
      }

      /* HERO: Left aligned, image bleeds right */
      .hero-wrapper { min-height: auto; }
      .hero {
        display: flex; flex-direction: row;
        min-height: 75vh;
        position: relative; overflow: hidden; align-items: stretch;
      }
      .hero-image-side { position: absolute; right: 0; top: 0; width: 65%; height: 100%; }
      .hero-image-side img { object-position: center top; }
      .hero-image-side::before {
        content: ''; position: absolute; inset: 0; z-index: 1;
        background: linear-gradient(to right, #0e0e0e 20%, rgba(14,14,14,0.85) 50%, rgba(14,14,14,0.1) 100%);
      }
      .hero-content {
        position: relative; z-index: 2; width: 100%;
        padding: 40px 24px;
        display: flex; flex-direction: column;
        justify-content: center; align-items: flex-start; text-align: left;
      }
      .badge { margin-bottom: 16px; font-size: 11px; padding: 6px 14px; }
      .hero-headline { font-size: clamp(28px, 8vw, 36px); line-height: 1.15; margin-bottom: 14px; max-width: 65vw; }
      .hero-desc { font-size: 13px; line-height: 1.6; margin-bottom: 24px; max-width: 65vw; color: rgba(255,255,255,0.7); }
      .hero-buttons { flex-direction: column; gap: 12px; width: min(280px, 70vw); }
      .btn-primary { width: 100%; justify-content: center; padding: 15px 20px; font-size: 14px; border-radius: 12px; }
      .btn-secondary { width: 100%; justify-content: flex-start; padding: 14px 20px; font-size: 14px; border-radius: 12px; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.12); }

      /* TRUST BADGE STRIP */
      .stats-bar {
        display: flex; flex-direction: row; flex-wrap: nowrap;
        overflow-x: auto; background: #141414;
        border-top: 1px solid rgba(255,255,255,0.05);
        border-bottom: 1px solid rgba(255,255,255,0.05);
        -webkit-overflow-scrolling: touch; scrollbar-width: none;
      }
      .stats-bar::-webkit-scrollbar { display: none; }
      .stat-item {
        flex: 0 0 auto; min-width: 45vw; padding: 16px 20px;
        border-right: 1px solid rgba(255,255,255,0.05); border-bottom: none;
        gap: 12px; flex-direction: column; align-items: flex-start;
      }
      .stat-icon-wrap { width: 40px; height: 40px; }
      .stat-number { font-size: 14px; }
      .stat-label  { font-size: 12px; }

      /* PROCESS */
      .process-section { flex-direction: column; }
      .process-img-side { position: relative; width: 100%; height: 240px; border-radius: 16px; overflow: hidden; }
      .process-content { width: 100%; padding: 32px 0 0; align-items: flex-start; text-align: left; }
      .process-headline { font-size: 26px; }

      /* SERVICES */
      .services-headline { text-align: left; font-size: 26px; margin-bottom: 8px; }
      .services-subtext  { text-align: left; font-size: 13px; margin-bottom: 24px; max-width: 90%; }
      .services-grid {
        display: flex !important; flex-wrap: nowrap !important;
        overflow-x: auto !important; gap: 16px !important;
        padding: 8px 24px 24px !important; margin: 0 -24px !important;
        -webkit-overflow-scrolling: touch; scrollbar-width: none;
      }
      .services-grid::-webkit-scrollbar { display: none; }
      .svc-card { flex: 0 0 72vw !important; border-radius: 16px; padding: 20px 16px; min-height: 0; }
      .svc-card-photo { display: none; }
      .svc-card-body  { padding: 0; }
      .svc-icon-wrap  { width: 40px; height: 40px; margin-bottom: 12px; }
      .svc-card-title { font-size: 14px; font-weight: 600; line-height: 1.3; margin-bottom: 8px; }
      .svc-card-desc  { font-size: 12px; line-height: 1.5; }
      .svc-learn-more { display: none; }

      /* WORKS */
      .works-label  { justify-content: flex-start; margin-bottom: 16px; }
      .works-headline { font-size: 26px; }
      .works-grid {
        display: flex !important; flex-wrap: nowrap !important;
        overflow-x: auto !important; gap: 16px !important;
        padding: 8px 24px 24px !important; margin: 0 -24px !important;
        -webkit-overflow-scrolling: touch; scrollbar-width: none;
      }
      .works-grid::-webkit-scrollbar { display: none; }
      .works-card { flex: 0 0 85vw !important; border-radius: 16px; }

      /* TESTIMONIALS */
      .testi-headline, .testi-subtext { text-align: left; }
      .testi-headline { font-size: 26px; }
      .testi-grid {
        display: flex !important; flex-wrap: nowrap !important;
        overflow-x: auto !important; gap: 16px !important;
        padding: 8px 24px 24px !important; margin: 0 -24px !important;
        -webkit-overflow-scrolling: touch; scrollbar-width: none;
      }
      .testi-grid::-webkit-scrollbar { display: none; }
      .testi-card { flex: 0 0 85vw !important; border-radius: 16px; padding: 24px; }

      /* EXPERT CALL BAR */
      .mob-expert-bar {
        display: flex; align-items: center;
        background: linear-gradient(135deg, #2a2000, #140e00);
        border: 1px solid rgba(212,160,23,0.25);
        border-radius: 16px; padding: 16px;
        margin: 0 24px 40px; gap: 14px;
      }
      .mob-expert-icon {
        width: 44px; height: 44px; border-radius: 50%;
        background: rgba(212,160,23,0.15); border: 1px solid rgba(212,160,23,0.3);
        display: flex; align-items: center; justify-content: center; flex-shrink: 0;
      }
      .mob-expert-icon svg { width: 22px; height: 22px; stroke: var(--gold); fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
      .mob-expert-text  { flex: 1; min-width: 0; }
      .mob-expert-title { font-size: 14px; font-weight: 700; color: #fff; margin-bottom: 4px; }
      .mob-expert-sub   { font-size: 11px; color: #9aa0b8; }
      .mob-expert-sub span { color: var(--gold); font-weight: 500; }
      .mob-call-now {
        background: #fff; color: #111; font-size: 13px; font-weight: 700;
        padding: 12px 16px; border-radius: 12px;
        text-decoration: none; white-space: nowrap; flex-shrink: 0;
      }

      /* ABOUT */
      .about-section { display: flex; flex-direction: column; }
      .about-headline { font-size: 26px; }
      .about-right   { order: -1; min-height: 280px; border-radius: 16px; margin-bottom: 24px; }
      .about-stats   { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
      .about-stat    { background: #1a1a1a; border: 1px solid rgba(255,255,255,0.05); border-radius: 16px; padding: 20px 16px; }

      /* MEDIA */
      .media-section { flex-direction: column; align-items: flex-start; }
      .media-headline { font-size: 26px; }
      .media-left  { max-width: 100%; margin-bottom: 32px; }
      .media-right { width: 100%; }
      .media-stats-bar { flex-direction: row; flex-wrap: wrap; gap: 0; border-left: none; padding-left: 0; }
      .media-stat  { flex: 0 0 50%; padding: 20px 16px; border-right: none; border-bottom: 1px solid rgba(255,255,255,0.05); }

      /* CTA BARS */
      .cta-bar      { flex-direction: column; align-items: center; text-align: center; padding: 32px 24px; border-radius: 16px; gap: 16px; }
      .cta-bar-btn  { width: 100%; justify-content: center; padding: 15px; border-radius: 12px; }
      .presskit-bar { flex-direction: column; text-align: center; padding: 32px 24px; border-radius: 16px; gap: 16px; }

      /* FOOTER */
      .site-footer { padding: 48px 24px 24px; margin-top: 0; }
      .footer-top  { display: flex; flex-direction: column; gap: 32px; }

      /* BOTTOM TAB BAR */
      .mobile-bottom-nav {
        display: flex; position: fixed; bottom: 0; left: 0; right: 0;
        height: 68px; padding-bottom: env(safe-area-inset-bottom, 0px);
        background: rgba(15, 15, 15, 0.95);
        backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
        border-top: 1px solid rgba(255,255,255,0.08);
        z-index: 9999; align-items: center; justify-content: space-around;
        box-shadow: 0 -4px 30px rgba(0,0,0,0.5);
      }
      .mob-tab {
        display: flex; flex-direction: column; align-items: center; justify-content: center;
        gap: 5px; flex: 1; height: 100%;
        text-decoration: none; color: #6a6a7a;
        font-size: 10.5px; font-weight: 500;
        transition: color 0.2s; -webkit-tap-highlight-color: transparent;
      }
      .mob-tab svg { width: 22px; height: 22px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
      .mob-tab.active { color: var(--gold); }
      .mob-tab.active svg { stroke: var(--gold); }
    }
